///|
/// Compatibility name retained for callers compiled against the legacy host
/// contract. New executor code should use `ProcessResult`.
#deprecated("Use ProcessResult")
pub type ProcessOutput = ProcessResult
