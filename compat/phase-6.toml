schema_version = 1
phase = 6
upstream = "just 1.57.0"
status = "implemented"
plan_exit = "pending-remote-ci-and-audit"
report = "docs/PHASE_6_REPORT.md"

[contracts]
arguments = "69-option/command inventory with requires, conflicts, aliases, validated formats, and stable unsupported errors"
files = "check, format, stdin output, and no-overwrite init through explicit HostFs"
queries = "list, show, summary, usage, variables, and groups without process capability"
serialization = "evaluate, dump, and schema-versioned stable JSON"
wasm = "read-only HostFs, deny-by-default moonrun policy, exact stdout/stderr, and no process adapter"

[target_matrix]
native = "pass: 130 tests"
wasm1 = "pass: 129 tests"
wasm_policy = "pass: list succeeds; format write and effectful evaluate fail without mutation"
native_platforms = "pending remote Ubuntu/macOS/Windows CI"

[evidence]
native_tests = 130
wasm_tests = 129
cli_inventory = "compat/cli-options.toml"
implemented_options = 14
implemented_commands = 11
json_schema_version = 1
matching_baseline_differentials = 4
wasm_policy = "policies/inspect.toml"
wasm_e2e = "tools/check_phase6_inspect.sh"
query_oracle = "tools/check_phase6_oracle.sh"
upstream_corpus = "tests/upstream/just-1.57.0/phase-6.toml"
upstream_covered_registrations = 138
upstream_excluded_registrations = 35
provenance = "tests/upstream/NOTICE.md"
