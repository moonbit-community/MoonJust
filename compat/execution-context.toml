schema_version = 2
area = "execution-context"
upstream = "just 1.57.0"
status = "strict-remediation"
plan_exit = "blocked-by-strict-evidence"
report = "docs/reports/PHASE_7_REPORT.md"
audit = "docs/reports/PHASE_7_REPORT.md"

[contracts]
filesystem = "Native and policy-aware wasm1 atomic replace, no-overwrite, canonicalization, permissions, and cleanup"
dotenv = "redacted dotenvy-compatible parser, ordered discovery, ambient precedence, required mode, and command sources"
invocation = "positional, variadic, recipe option, count, pattern, expression, usage, and stable error model"
working_directory = "explicit invocation, project, import, module, evaluation, and recipe working directories"
environment = "CLI variable overrides, shell selection, tempdir, platform rules, and child environment precedence"
executor_boundary = "invocations and process configuration are validated without granting process execution"

[target_matrix]
native = "pass: 246 tests"
wasm1 = "pass: 243 tests"
native_platforms = "pass: Ubuntu/macOS/Windows on audited main run 31244990807"
wasm_policy = "pass: writable transaction policy is isolated from the read-only inspect policy"

[evidence]
native_tests = 246
wasm_tests = 243
hostfs_gate = "tools/verification/checks/hostfs.sh"
dotenv_gate = "tools/verification/checks/dotenv.sh"
invocation_gate = "tools/verification/checks/invocation.sh"
working_directory_gate = "tools/verification/checks/workdir.sh"
environment_gate = "tools/verification/checks/environment.sh"
upstream_corpus = "tests/upstream/just-1.57.0/execution-context.toml"
upstream_covered_registrations = 188
upstream_unverified_registrations = 0
provenance = "tests/upstream/NOTICE.md"
